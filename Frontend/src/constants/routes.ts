export const ROUTES = {
  root: '/',
  login: '/login',
  projects: '/projects',
  projectDetail: '/projects/:projectId',
  scanDetail: '/scans/:scanId',
  resultDetail: '/results/:scanId',
} as const;
