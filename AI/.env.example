# ── Ollama ──
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:3b
OLLAMA_TEMPERATURE=0.1
OLLAMA_TIMEOUT_SECONDS=600
OLLAMA_MAX_RETRIES=2
OLLAMA_RETRY_BACKOFF_SECONDS=1

# ── LLM Provider ──
# ollama | anthropic
LLM_PROVIDER=ollama

# ── Anthropic ──
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-3-5-haiku-latest
ANTHROPIC_TEMPERATURE=0.1
ANTHROPIC_TIMEOUT_SECONDS=600

# ── AWS S3 ──
AWS_REGION=ap-northeast-2

# 기본 bucket입니다. 별도 bucket env가 없으면 raw scan과 analysis result 모두 이 값을 사용합니다.
AWS_S3_BUCKET=ssafer-scan-storage-dev

# scan_result.json 다운로드 bucket입니다.
APP_SCAN_RESULT_S3_BUCKET=ssafer-scan-storage-dev

# analysis_result.json 업로드 bucket입니다.
APP_ANALYSIS_RESULT_S3_BUCKET=ssafer-scan-results-dev
S3_MAX_RETRIES=2
S3_RETRY_BACKOFF_SECONDS=1

# Worker
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VIRTUAL_HOST=/
AGENT_TASK_SCAN_REQUEST_QUEUE=ssafer.agent.scan.request
FASTAPI_BASE_URL=http://127.0.0.1:8000
SPRING_BASE_URL=http://127.0.0.1:8080
SPRING_API_SECRET=
WORKER_HTTP_TIMEOUT_SECONDS=120

# AWS credentials입니다. 실제 값은 커밋하지 않습니다.
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# LocalStack/MinIO 같은 S3 호환 저장소를 사용할 때만 설정합니다.
# AWS_S3_ENDPOINT_URL=http://localhost:4566
